<div class="container py-4">

  <!-- Page Header -->
  <div class="d-flex justify-content-between align-items-center mb-4">
    <div>
      <h1 class="h3 mb-0">Dashboard</h1>
      <p class="text-muted mb-0">Welcome back, <%= Current.user.display_name %></p>
    </div>
  </div>

  <!-- Metric Cards -->
  <div class="row g-3 mb-4">
    <div class="col-md-4">
      <div class="card shadow-sm border-0">
        <div class="card-body">
          <h6 class="text-muted">Users</h6>
          <h2 class="fw-bold"><%= User.count %></h2>
        </div>
      </div>
    </div>

    <!-- <div class="col-md-4">
      <div class="card shadow-sm border-0">
        <div class="card-body">
          <h6 class="text-muted">Assessments</h6>
          <h2 class="fw-bold"><%#= Assessment.count %></h2>
        </div>
      </div>
    </div>

    <div class="col-md-4">
      <div class="card shadow-sm border-0">
        <div class="card-body">
          <h6 class="text-muted">Courses</h6>
          <h2 class="fw-bold"><%#= Course.count %></h2>
        </div>
      </div>
    </div>
  </div> -->

  <!-- Lower Section -->
  <div class="row g-3">
    <div class="col-lg-8">
      <div class="card shadow-sm border-0">
        <div class="card-header bg-white">
          <strong>Recent Activity</strong>
        </div>
        <div class="card-body">
          <p class="text-muted">No recent activity yet.</p>
        </div>
      </div>
    </div>

    <div class="col-lg-4">
      <div class="card shadow-sm border-0">
        <div class="card-header bg-white">
          <strong>Quick Actions</strong>
        </div>
        <div class="card-body">
          <div class="d-grid gap-2">
            <%#= link_to "Create Assessment", new_assessment_path, class: "btn btn-primary" %>
            <%#= link_to "Manage Users", users_path, class: "btn btn-outline-secondary" %>
          </div>
        </div>
      </div>
    </div>
  </div>

</div>
